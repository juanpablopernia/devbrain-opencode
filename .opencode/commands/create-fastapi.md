---
description: Crea o prepara estructura base FastAPI con .env, README y módulos iniciales.
agent: backend-python
---

Crea una base de proyecto FastAPI siguiendo las skills `python-fastapi-backend` y `python-env-config`.

Antes de escribir:

1. Revisa `docs/brain/CONTEXT_INDEX.md` si existe.
2. Revisa `docs/brain/PROJECT_BLUEPRINT.md`.
3. Confirma si el proyecto necesita IA, base de datos o frontend.
4. Si falta información crítica, pregunta.

Entregables esperados:

- Estructura `app/`.
- `app/main.py`.
- `app/config.py`.
- `app/routes/`.
- `app/services/`.
- `app/schemas/`.
- `.env.example`.
- `requirements.txt` o `pyproject.toml`.
- README con ejecución local.

Argumentos:

$ARGUMENTS
